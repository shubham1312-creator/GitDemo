Git >> Version Control

    v1
    v2
    v3
new v4


  >> It is distributed (on cloud)
  >> Collabration
  >> Tracking Changes
  >> Recovery

  https://git-scm.com/downloads

  Github
  Bitbucket
  Gitlabs
  Gitpod

  >> for 1st time need to configure git in our system
   
   git config --global user.name "shubham1312-creator"
   git config --global user.email "shubhamuttarwar4@gmail.com" 

   git config --global color.ui auto 

   git init
   git remote add origin https://github.com/shubham1312-creator/GitDemo.git


git pull origin main 
git status

git add .
git status
git commit -m "commitmessage"
git push

if u have to create a new branch
git checkout -b "branchname"